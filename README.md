# This is a script to remind you to ID yourself.

First you will need to install Zenity. This allows you to display graphical dialog boxes from shell scripts.

Once you have installed Zenity download the id_reminder.sh script.

Next make the script executable by running '''chmod +x id_reminder.sh'''

Finally run the script ```./id_reminder.sh &```
